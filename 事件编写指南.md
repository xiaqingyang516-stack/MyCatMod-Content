# MyCatMod 事件编写指南

## 选项分支的正确写法

### quickQuestion（推荐，适用于任意数量的选项）

`quickQuestion` 是最简洁的分支方式，所有分支内容写在一行里，选完后自动汇合到后续剧情。

**格式：**
```
quickQuestion #提示文本#选项1#选项2#选项N(break)分支1指令(break)分支2指令(break)分支N指令
```

**规则：**
- `#` 分隔提示文本和各选项
- `(break)` 分隔每个选项对应的指令
- 分支内部的指令用 `\` 分隔（不是 `/`，因为 `/` 是事件级别的分隔符）
- 所有分支结束后，事件自动继续执行 `/` 后面的命令

**JSON 转义（重要！）：**
- 分支内命令分隔符：游戏里是 `\\`，JSON 里写成 `\\\\`（四个反斜杠）
- `"` 在 JSON 里写成 `\"`
- 注意：`\\` 只替代 `/` 作为分支内的命令分隔符，分支结束后回到 `/`

**示例（2 个选项）：**
```json
"quickQuestion #你想说什么？#选项A#选项B(break)speak MaoMao \\\"选A的回应\\\"\\\\emote MaoMao 32(break)speak MaoMao \\\"选B的回应\\\"/pause 500/speak MaoMao \\\"后续共享剧情\\\"/end"
```

**示例（3 个选项）：**
```json
"quickQuestion #你想说什么？#选项A#选项B#选项C(break)speak MoMo \\\"回应A\\\"(break)speak MoMo \\\"回应B\\\"(break)speak MoMo \\\"回应C\\\"/pause 500/end"
```

---

### question + fork（仅适用于 2 个选项）

**重要：`question` 的 key 必须是 `forkN` 格式（N = 触发跳转的选项编号，从 0 开始），否则分支逻辑完全不生效！**

**格式：**
```
question fork1 "提示文本#选项0#选项1"/fork 跳转目标/选项0的内联剧情.../end
```
然后在 Entries 里额外定义跳转目标：
```
"跳转目标": "选项1的剧情.../end"
```

**工作原理：**
- `question fork1` 中的 `1` 表示：玩家选第 2 个选项时触发跳转
- 选第 1 个选项（index 0）→ 跳过 fork，继续执行后面的内联内容
- 选第 2 个选项（index 1）→ 跳转到 fork 指定的事件 key

**示例：**
```json
{
  "eventId/preconditions": "...setup.../question fork1 \"提示#留下来吧#我再想想\"/fork try_branch/speak MaoMao \"太好了！\"/end",
  "try_branch": "speak MaoMao \"好吧，那先这样。\"/end"
}
```

---

## 错误写法（不要用！）

```json
"question momo_adopt \"文本#选项1#选项2\"/fork momo_adopt/end",
"momo_adopt_0": "选项1的内容.../fork momo_adopt_end",
"momo_adopt_1": "选项2的内容.../fork momo_adopt_end",
"momo_adopt_end": "共享结尾.../end"
```

**错误原因：**
1. `question momo_adopt` 的 key 不含 `fork` 关键字，引擎完全忽略分支逻辑
2. `_0` / `_1` 后缀命名不是引擎识别的格式，fork 只跳转到你指定的那一个 key
3. 分支内使用 `fork xxx_end` 跳转共享结尾 — 但 fork 依赖 `specialEventVariable1` 状态，不能当无条件跳转用

---

## question null（无分支，纯装饰性选项）

如果两个选项结果完全相同，只是让玩家表达态度，可以用 `question null`：

```json
"question null \"提示#选项A#选项B\"/speak MaoMao \"后续剧情（无论选哪个都一样）\"/end"
```

---

## 所有事件 ID 一览（测试用）

在 SMAPI 控制台输入 `debug ebi <事件ID>` 即可触发对应事件。

| 事件 ID | 地点 | 描述 | 前置条件 |
|---------|------|------|---------|
| `75160001` | BusStop | 毛毛收养事件 | 毛毛好感 250+ |
| `75160011` | Farm | 墨墨收养事件 | 已看过毛毛收养事件 |
| `75160002` | Farm | 毛毛 2 心事件 | 毛毛+墨墨好感 500+ |
| `75160004` | Forest | 毛毛 4 心·秘密基地 | 毛毛+墨墨好感 1000+ |
| `75160006` | Town | 毛毛 6 心事件 | 毛毛好感 1500+ |
| `75160062` | Railroad | 墨墨 6 心事件 | 墨墨好感 1500+ |
| `75160008` | Forest | 毛毛 8 心·巫师相遇 | 毛毛+墨墨好感 2000+ |
| `75160082` | Farm | 毛毛 8 心·护身符出发 | 已看过巫师事件 + 持有护身符 |
| `75160083` | WizardHouse | 毛毛 8 心·勇气考验结果 | 已看过护身符出发 + 收到完成邮件 |
| `75160010` | Mountain | 毛毛 10 心事件 | 毛毛+墨墨好感 2500+ |
| `75160020` | Woods | 茉莉花茶事件 | 毛毛+墨墨好感 500+ |
